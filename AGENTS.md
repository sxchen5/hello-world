# AGENTS.md

## Cursor Cloud specific instructions

This is a bare "hello-world" repository containing only a `README.md` file. There are:

- **No application services** to run
- **No dependencies** to install
- **No build system**, linter, or test framework configured
- **No Docker or docker-compose** configuration

If application code is added in the future, update this section with relevant build/run/test instructions.
